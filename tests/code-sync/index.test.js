describe('code-sync', () => {
  test('A client joins a dojo session')
  test('Two clients joins the dojo session')
  test('Only one client is allowed to push code')
  test('A client pushes code to all clients in dojo session')
})